<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  AutoGenerated by lindzh  2016年12月15日 16:55 -->
<mapper namespace="com.lindzh.hasting.webui.dao.ServiceProviderInfoDao">

	<resultMap id="ServiceProviderInfoResultMap" type="com.lindzh.hasting.webui.pojo.ServiceProviderInfo">
		<id property="id" column="id"/>
		<result property="serviceId" column="service_id"/>
		<result property="appId" column="app_id"/>
		<result property="hostId" column="host_id"/>
		<result property="time" column="provider_host_uptime"/>
	</resultMap>
	
	<insert id="addServiceProviderInfo" useGeneratedKeys="true" keyProperty="id" parameterType="com.lindzh.hasting.webui.pojo.ServiceProviderInfo">
		insert into service_provider_info(service_id,app_id,host_id,provider_host_uptime)
		 values(#{serviceId},#{appId},#{hostId},#{time})
	</insert>
	
	<select id="getById" resultMap="ServiceProviderInfoResultMap">
		select * from service_provider_info where id=#{id}
	</select>
	
	<update id="updateById" parameterType="com.lindzh.hasting.webui.pojo.ServiceProviderInfo">
		update service_provider_info set service_id=#{obj.serviceId},app_id=#{obj.appId},host_id=#{obj.hostId},provider_host_uptime=#{obj.time}
		where id=#{obj.id}
	</update>
	
	<delete id="deleteById">
		delete from service_provider_info where id=#{id}
	</delete>

	<delete id="deleteByAppIdAndHostId">
		delete from service_provider_info where app_id=#{appId} and host_id=#{hostId}
	</delete>

	<select id="getByAppHostAndServiceId" resultMap="ServiceProviderInfoResultMap">
		select * from service_provider_info where
		service_id=#{serviceId} and app_id=#{appId} and host_id=#{hostId}
	</select>

	<select id="getServiceProviderCount" resultType="long">
		select count(*) from service_provider_info where
		service_id=#{serviceId} and app_id=#{appId}
	</select>
	

</mapper>